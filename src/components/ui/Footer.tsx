"use client"
import { Layout } from 'antd';

const { Header, Content, Footer } = Layout;
const MainFooter = () => {
  return (
    <Footer style={{ textAlign: 'center' }}>CleanTech ©2023</Footer>

  );
};

export default MainFooter;
